# 贡献指南 · Contributing

感谢关注 orangeink（橙墨）。这是一个单文件、零依赖的离线工具，贡献门槛很低——但也正因为单文件，请先读完这份指南再动手。

## 提 Issue

- **兼容性坑**（最高价值）：粘贴到公众号后出现的任何异常，请附上**可复现的最小 HTML 片段**（复制出来的 payload 更好）。微信的解析行为是黑盒，每一个实弹坑都值得进 [docs/wechat-compat.md](docs/wechat-compat.md)；
- **功能建议**：请说清「它解决什么阅读任务」——读者在手机上刷到这段内容时，这个功能让什么变得更清楚了。纯换皮的样式变种大概率不会被接受；
- **主题投稿**：见下方「贡献新主题」。

## 贡献新主题

一个主题 = 一个 JS 对象。步骤：

1. 打开 `src/template.html`，找到 `THEMES` 注册表，仿照现有四主题写一个新条目（键名用英文、`name` 用中文显示名）；
2. **硬性规则**（过不了就不收）：
   - 所有**文字色**相对所在背景 ≥ WCAG AA（4.5:1）；大字号 ≥3:1；
   - `ORANGE`（accent）**只做条/点/序号等装饰，永远不做正文文字色**；
   - 徽章底色（`CHIP` 等）与白字 ≥4.5:1；
3. 本地跑通自检：`python src/build.py` 后打开 `index.html`，粘贴一篇覆盖全要素的测试文，点「自检」直到 **0 项**；
4. 提交 PR，附：色板表 + 对比度计算过程 + 预览截图（手机 375 视图）。

色板各键位的含义见 [docs/theming.md](docs/theming.md)。

## 贡献模块（`:::directive`）

模块管结构、主题管气质——新模块必须是**新的阅读任务**（如数据行、结构预告、左右对比），而不是既有模块的换皮。PR 请附：

- 语法提案（`:::name[key]` + `key: value` 行）；
- 渲染示例的 HTML（公众号粘贴实测过的截图最好）；
- 不使用 flex/grid（公众号会过滤，用 table 布局保稳）。

## 本地开发

```bash
# 无需 npm install，只需 Python 3
python src/build.py     # 产出 index.html
```

改完请至少验证：

1. 深浅两主题下自检全绿；
2. 复制 payload 无空标签、无 font-family、行高全部 px；
3. 手机 375 视图目检折行。

## 提交规范

- commit message 用祈使句：`add: 松烟主题的 H4 色`、`fix: 引用块底部分隔线丢失`；
- 单 PR 单主题/单修复，别混。
